# Принятые риски для Trivy gate. Каждая запись — с обоснованием и сроком (exp:),
# после которого Trivy снова начнёт блокировать сборку.

# ОС-пакеты базового образа distroless/java21-debian12: исправление есть в Debian,
# но образ distroless ещё не пересобран. Приложение не использует эти библиотеки
# (разбор XML — через JDK, обработка цветовых профилей не вызывается).
# Проверить новый образ distroless к сроку; Dependabot предложит обновление.
CVE-2026-76957 exp:2026-10-31
CVE-2026-41254 exp:2026-10-31
